spring.jpa.hibernate.ddl-auto=update
spring.flyway.enabled=false
logging.level.com.chatapp=DEBUG

# Override DB port — Docker postgres runs on 5433 to avoid conflict with local PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5433/chatdb

# Allow local file:// origin testing
app.websocket.allowed-origins=*
